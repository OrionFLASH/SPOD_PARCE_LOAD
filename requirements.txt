# Зависимости main.py — справочно и для разработки. На рабочей машине ничего не устанавливается:
# используется Anaconda 3.12 (pandas, numpy, openpyxl входят в base), pip install запрещён.
# Минимальные версии = проверенное окружение Anaconda 3.12 (Python 3.12.7, pandas 2.2.2,
# numpy 1.26.4, openpyxl 3.1.5). Проверка окружения без установки: python -m src.runtime_env
# Запись Excel-формы BADGE: только stdlib (zipfile/xml), без xlsxwriter.
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
