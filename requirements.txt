# Зависимости для main.py (чтение CSV, обработка, запись Excel).
# Для базового Python 3.10+ — pip install -r requirements.txt
# Для Anaconda 3.12 — pandas/openpyxl/numpy обычно уже в base.
# Запись Excel-формы BADGE: только stdlib (zipfile/xml), без xlsxwriter.
et_xmlfile==2.0.0
numpy==2.3.3
openpyxl==3.1.5
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
tzdata==2025.2
