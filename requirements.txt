# Зависимости для main.py (чтение CSV, обработка, запись Excel).
# Для базового Python 3.10 — обязательны: pip install -r requirements.txt
# Для Anaconda 3.10 — обычно уже в base (numpy, pandas, openpyxl).
et_xmlfile==2.0.0
numpy==2.3.3
openpyxl==3.1.5
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
tzdata==2025.2
