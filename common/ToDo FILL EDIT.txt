Составь на основании этих пожелайний ToDo + RoudMAp + Plan 
доработок для страницы fill_full (и возможно edit_full)

При необходимости задай дополнительные вопросы

Статус волны 20 (fill-full + edit-full): всё ниже — [v].
План/чеклист: Docs/PLAN_WEB_FILL_EDIT_WAVE20.md, Docs/TODO_WEB_FILL_EDIT_WAVE20.md, ROADMAP п.20.



1) [v] Поправить текст в EDIT и FILL: 

CONTEST_FEATURE.masking
с "Всегда ставами в Нет (N)" 
на "Всегда ставим в Нет (N)"

CONTEST_FEATURE.tournamentRewardingMailing
вместо "РАссылка письма получателям награды после ее вручения"
ставим "Рассылка письма получателям награды после ее вручения"

→ Как: description в game_edit_catalog.json (+ sync в web-fill-full/catalog).

2) [v] CONTEST_FEATURE.feature, REWARD_ADD_DATA.preferences и другие поля с типом "Массив" если предусмотрен ввод ручной данных
Каждый отдельный элмент массива записывается с новой строки. те разделитель не ; а перенос строки

→ Как: listFromCell / listToCell + mountListFreeform (textarea, split по \n; ; при разборе — совместимость).

3) [v] в разделе GROUP (не INDICATOR) Методы и пороги: первая строка GET_CALC_METHOD; вторая GET_CALC_CRITERION + ADD_CALC_CRITERION + ADD_CALC_CRITERION_2

→ Как: GROUP_LAYOUT в game_fill_core.js (span 12 / 4+4+4).

4) [v] REWARD_ADD_DATA.fileName
меняем на "можно пусто"

→ Как: allow_empty: true в каталоге edit → sync fill.

5) [v] Название бизнес блока везде меняем в edit и full
с "КМ ММБ" ("KMMMB")
на "МКК ММБ" ("MKKMMB")

→ Как: variants/labels/default в каталоге; BUSINESS_BLOCK_NAMED + миграция KMMMB→MKKMMB при импорте fill.

6) [v] везде где есть сезоны season надо поменять
c "SEASON_mmb_2026"
на "SEASON_mkk_2026"

→ Как: variants/default в каталоге; миграция SEASON_mmb_2026→SEASON_mkk_2026 при импорте.

7) [v] для fill 
в исходных данных JSON импортируемых могут отсутствовать ключи которые помечены как обязательные
а также могут быть не заполнены поля отмеченные как "можно пусто - НЕТ"

в таком случае в интерфейсе ключ надо в редактирвании отметить например красным кружком на названии поля и текстом предупредления что нет ключа или не заполнено
при этом в кнопках выбора которые не допускают отжатие обоих тк значнеие должно быть надо варианты подсветить (символизируя) что все же выбрать надо но при этом пока не выбирать.

если выбор так и не сделан то при сохранении ключ должен появиться, но данных в нем пока не будет
те в таких случаях за пользователя выбор не делаем. но обязательный ключ в JSON добавляем

→ Как: fieldRequirementIssue + req-warn; chips is-need-pick без автовыбора; packJsonLeaves пишет обязательный ключ даже пустым.

8) [v] в fill выделение активной строки конкурса делай более явно. сейчас не видно что выделена та с которой работаем

→ Как: CSS .contest-tab.active — фон/обводка/тень.

9) [v] в заголовке в верхнем колонтитуле где "Конкурс карточка + FEATURE"
надо заменить карточка + FEATURE - на этом месте пишем "ТУРНИР" если CONTEST_TYPE = ТУРНИРНЫЙ
или "НАГРАДА" если CONTEST_TYPE = ИНДИВИДУАЛЬНЫЙ или ИНДИВИДУАЛЬНЫЙ НАКОПИТЛЬНЫЙ

а там где был текст ТУРНИРНЫЙ или ИНДИВИД.... 
на самой кнопке вверху конкурса. надо писать КОД КОНКУРСА CONTEST_CODE

→ Как: contestGroupMetaLabel + contestNavButtonTitle в game_fill_model.js / filters.

10) [v] REWARD_ADD_DATA.nftFlg поменять описание
на "Ставить метку NFT на награду / турнир?"

→ Как: description в каталоге + sync.

11) [v] важно. 
'[{"""period_code""": 0, """criterion_mark_type""": """>""", """criterion_mark_value""": 0}]'
вот этот параметр в CONTEST 
при сохранении не верные кавычки добавляются вокруг JSON 
сейчас добавляется ' с двух сторон, а долна добавляться " кавычка двойная
сделай замену здесь и в других таких местах где есть

Также здесь же проверь, поле вооще не должно было в CSV появиться.
тк в UI нет ни одного добавленного Периода расчета. а ты как-то добавил нулевые данные как выше
В этом поле когда не заполнены Наборы надо в CSV (и наверное в JSON) сохранять пусто в этом поле CONTEST_PERIOD
Пусто в этом случае это [] без кавычек без внутненностей и без кавычек вокруг - это пустое

Проверь кореркность такого заполенния при отсутсвии значений в UI в JSON в других местах тоже

→ Как: убран фантом в contestPeriodPacked; dumpsSpodJsonArrayCell по каталогу (empty_json_mode=brackets → []; wrap `"…"` для непустого; без `'`).
→ PY consistency (ROADMAP 21): json_spod_format запрещает внешнюю обёртку `'…'` во всех режимах с проверками (main_only / consistency_only / full).

12) [v] в связи с 11 пунктом доработай EDIT надо там указывать для типа JSON и массив как выглядит пустое поле
будет ли это просто пусто в поле
или будет это вот такие [] 
сделай кнопку выбора
и потом на основании этих данных будешь формировать fill (переделаешь)
и можно добавить надо или нет такие поля но с данными внутни в целом обрамлять кавычкой "

продумай как это настроить можно в edit и потом корректно перносить в fill и также потом в fill корректно формировать JSON и CSV

→ Как: edit-full UI empty_json_mode (пусто / [] / "[]") + json_wrap_quotes; sync → fill читает только каталог.

13) [v] Проверь что если ключ или параметр имеет тип массив то оно при сохранении в CSV и/или JSON должно быть массивом
например
"""seasonItem""": """SEASON_2026_1""",  - так не верно
"""seasonItem""": ["""SEASON_2026_1"""] - надо вот так

тоже самое 
"""helpCodeList""": [] - так ОК
"""helpCodeList""": ["""NFT_1"""]- так тоже ОК
"""helpCodeList""": """NFT_1""" так не верно

проверь эти два поля в EDIT и FILL  как задано как сохраняем

→ Как: kind=list (в т.ч. seasonItem); coerceSpodPackValue / packJsonLeaves всегда пишут […].
→ PY consistency (ROADMAP 21): array_value_keys helpCodeList / seasonItem в CONFIG_CHECKS (spod_json_contest_data, spod_json_reward_add_data).

14) [v] """seasonItem""": ["""SEASON_2026_1"""]
Этот ключ есть только у наград REWARD у которых REWARD_TYPE = ITEM 
и у некоторых но не всех REWARD_TYPE = BADGE
поэтому у такого клбюча надо проставить какой то признак что если он пустой то мы его в файлы CSV не включаем.

надо как-то это в EDIT помечать и в FILL доработать.

продумай как это отметить

→ Как: omit_when_empty на seasonItem в edit; fill не пишет ключ если пусто (ITEM/часть BADGE — через заполненность, без жёсткого only-ITEM).

15) [v] надо продумать как в EDIT задавать такие зависимые ключи которые есть только у определенных типов наград
те некое поле в котором указываем таблицу, поле, ключ если JSON в поле, и занчение 
те ключ описываемый будет только если вот то указанное ровно чему то
причем надо предусмотреть от 1 до 3 таких зависимостей

И далее это корректно перенсти и обарботаь в FILL

→ Как: depends_on[{table, field, json_key, equals}] ×1–3 AND в edit UI; packJsonLeaves / fieldDependsOk в fill.

16) [v] в папке IN - PROM - SPOD появились новые версии файлов PROM
пересоздай JSON файлы PROM и mixed (PROM+PSI) 
с учетом выше сделанных доработок

файлы: 
ORG_UNIT_V20 04-06 v2.csv
CONTEST (PROM) 20-08 v0.csv
EMPLOYEE (PROM) 02-07 v0.csv
GROUP (PROM) 19-08 v0.csv
INDICATOR (PROM) 19-08 v0.csv
REPORT (PROM) 17-08 v0.csv
REWARD (PROM) 21-08 v0.csv
REWARD-LINK (PROM) 19-08 v0.csv
SCHEDULE (PROM) 20-08 v0.csv
USER_ROLE (PROM) 21-08 v0.csv
USER_ROLE_SB (PROM) 21-08 v0.csv

→ Как: CONFIG PROM уже на 19–21.08; PSI → *_2026-07-09; export_web_fill_examples_from_spod.py → curated/badges/contests (+ PSI + merged).
