1) в режиме consistency_only — создаётся только файл SPOD_PROM CONSISTENCY (без SPOD_PROM source).
   Реализовано: source пишется только при run_mode == full (и отдельно при source_only до выхода).

2) для файла source — во всех колонках и листах по умолчанию включён перенос по словам (wrap_text).
   Реализовано в write_source_excel (Alignment wrap_text=True, vertical=top).
