1)
Лист                       Строки    Примечание
  -----------------------------------------------------------
  GROUP                      467       файл без изменений (SHA)
  INDICATOR                  345       файл без изменений (SHA)
  REWARD-LINK                627       файл без изменений (SHA)
  TOURNAMENT-SCHEDULE        636       new=636 chg=0 same=0 off=0
  ORG_UNIT_V20               128       файл без изменений (SHA)
  USER_ROLE SB               202       файл без изменений (SHA)

  в TOURNAMENT-SCHEDULE изменил всего 4 строки по коду ключу
  но мне система пишет каждый раз даже если изменилась 1 строка что изменились все!

  Проверь работу. напомуинаю что мы сейчас смотрим правки для базы по отдельным строкам

а в следующий запуск уже у другого файла
Лист                       Строки    Примечание
  -----------------------------------------------------------
  GROUP                      467       файл без изменений (SHA)
  INDICATOR                  345       файл без изменений (SHA)
  REWARD-LINK                627       файл без изменений (SHA)
  TOURNAMENT-SCHEDULE        636       new=0 chg=7 same=629 off=0
  ORG_UNIT_V20               128       файл без изменений (SHA)
  USER_ROLE                  202       файл без изменений (SHA)
  USER_ROLE SB               202       файл без изменений (SHA)
  EMPLOYEE                   716       файл без изменений (SHA)
  REPORT                     19927     new=19927 chg=0 same=0 off=0
  REWARD                     627       файл без изменений (SHA)
  CONTEST-DATA               360       файл без изменений (SHA)

2)
Задача по конфигу консистентности:
- Добавить в параметры консистентности настройку, где для каждой проверки можно указать, к каким блокам (PROM/IFT/PSI) её применять.
- Применять проверки консистентности к файлам блока только если этот блок указан у конкретной проверки.
- Сделать возможность задавать настройки, которые применяются только к определённым блокам/файлам блоков.
- Поведение по умолчанию: для всех проверок включены все блоки (если блоки явно не ограничены в конфиге).
