
---

# SPOD PROM DATA TOOL

## 📋 Описание проекта

**SPOD PROM DATA TOOL** — профессиональный Python-инструмент для автоматизированной интеграции, сверки, агрегации и анализа отчётных выгрузок по конкурсам, наградам и показателям системы геймификации. 

### 🎯 Основное назначение
Инструмент предназначен для обработки данных из системы SPOD (Сбербанк Платформа Организационного Развития) и формирования комплексных аналитических отчётов в формате Excel с автоматическим разворачиванием сложных JSON-структур и объединением данных из множественных источников.

---

## 🚀 Основные функциональные возможности

### ✅ Автоматическая обработка данных
- **Загрузка CSV-файлов** по конфигурации с автоматическим определением структуры
- **Разворачивание JSON-полей** любой глубины вложенности
- **Универсальные объединения (JOIN)** по любым ключам и их комбинациям
- **Формирование сводных таблиц** с гибкой настройкой связей

### ✅ Интеллектуальная обработка JSON
- **Автоматическое обнаружение** JSON-структур в колонках
- **Рекурсивное разворачивание** вложенных объектов и массивов
- **Корректная обработка** некорректных JSON-строк с автоисправлением
- **Поддержка любых уровней** вложенности (например: `ADD_DATA => getCondition => employeeRating => minRatingBANK`)

### ✅ Аналитические возможности
- **Детекция дубликатов** с маркировкой и подсчётом
- **Цветовое кодирование** колонок по типам данных
- **Форматирование Excel** с закреплением областей и автофильтрами
- **Подробное логирование** всех операций с временными метками

### ✅ Расширенные возможности обработки данных (НОВОЕ v1.7)
- **Размножение строк** при множественных совпадениях (`multiply_rows`)
- **Гибкое управление шириной колонок** с индивидуальными настройками
- **Динамическая цветовая схема** на основе правил объединения
- **Расширенное логирование** операций с детальной аналитикой

### ✅ Масштабируемость
- **Конфигурационный подход** — добавление новых источников без изменения кода
- **Универсальные правила объединения** — гибкая настройка связей между таблицами
- **Кроссплатформенность** — работа на Windows, macOS, Linux

---

## 📁 Структура проекта

```
SPOD_PARCE_LOAD/
├── main.py                 # Основной исполняемый файл
├── README.md              # Документация проекта
├── .gitignore             # Исключения для Git
├── venv/                  # Виртуальное окружение Python
├── DIR_INPUT/             # Входные CSV-файлы
│   ├── CONTEST-DATA (PROM) 2025-07-24 v4.CSV
│   ├── GROUP (PROM) 2025-07-14 v0.CSV
│   ├── INDICATOR (PROM) 2025-07-14 v0.CSV
│   ├── REPORT (PROM-KMKKSB) 2025-07-24 v4.CSV
│   ├── REWARD (PROM) 2025-07-24 v1.CSV
│   ├── REWARD-LINK (PROM) 2025-07-14 v0.CSV
│   ├── SVD_KB_DM_GAMIFICATION_ORG_UNIT_V20 2025_07_11 v1.CSV
│   ├── TOURNAMENT-SCHEDULE (PROM) 2025-07-24 v3.CSV
│   ├── PROM_USER_ROLE 2025-07-21 v0.CSV
│   ├── PROM_USER_ROLE SB 2025-07-21 v0.CSV
│   └── employee_PROM_final_5000.CSV
├── DIR_OUTPUT/            # Выходные Excel-файлы
│   └── SPOD_ALL_IN_ONE_YYYY-MM-DD_HH-MM-SS.xlsx
└── DIR_LOGS/              # Лог-файлы
    └── LOGS2_YYYY-MM-DD_HH-MM.log
```

---

## ⚙️ Конфигурация и настройки

### 📂 Директории проекта
```python
DIR_INPUT = r'/Users/orionflash/Desktop/MyProject/SPOD_PROM/SPOD'
DIR_OUTPUT = r'/Users/orionflash/Desktop/MyProject/SPOD_PROM/OUT'
DIR_LOGS = r'/Users/orionflash/Desktop/MyProject/SPOD_PROM/LOGS'
```

### 📄 Входные файлы (INPUT_FILES)
Массив конфигураций для каждого обрабатываемого CSV-файла с расширенными параметрами ширины:

```python
INPUT_FILES = [
    {
        "file": "CONTEST-DATA (PROM) 2025-07-24 v4",  # Имя файла без расширения
        "sheet": "CONTEST-DATA",                      # Имя листа в Excel
        "max_col_width": 120,                         # Максимальная ширина колонки
        "freeze": "C2",                               # Закрепление области (строка:колонка)
        "col_width_mode": "AUTO",                     # Режим растягивания: "AUTO", число, None
        "min_col_width": 8                            # Минимальная ширина колонки
    },
    # ... другие файлы
]
```

### 🔗 Правила объединения (MERGE_FIELDS) - ОБНОВЛЕНО
Универсальная система для объединения данных между листами с новыми возможностями:

```python
MERGE_FIELDS = [
    {
        "sheet_src": "CONTEST-DATA",           # Источник данных
        "sheet_dst": "REPORT",                 # Целевой лист
        "src_key": ["CONTEST_CODE"],           # Ключ(и) в источнике
        "dst_key": ["CONTEST_CODE"],           # Ключ(и) в целевом листе
        "column": ["CONTEST_TYPE", "FULL_NAME", "BUSINESS_STATUS", "BUSINESS_BLOCK"], # Колонки для переноса
        "mode": "value",                       # Режим: "value" или "count"
        "multiply_rows": False,                # 🆕 Размножать ли строки при множественных совпадениях
        "col_max_width": 80,                   # 🆕 Максимальная ширина добавляемых колонок
        "col_width_mode": "AUTO",              # 🆕 Режим растягивания для добавляемых колонок
        "col_min_width": 8                     # 🆕 Минимальная ширина для добавляемых колонок
    }
]
```

#### 🆕 Новые параметры MERGE_FIELDS:
- **`multiply_rows`** (bool): При `True` размножает строки при множественных совпадениях, при `False` берет первое найденное значение
- **`col_max_width`** (int/None): Максимальная ширина для добавляемых колонок
- **`col_width_mode`** (str/int): Режим растягивания (`"AUTO"`, число, `None`)
- **`col_min_width`** (int): Минимальная ширина для добавляемых колонок

### 🎨 Цветовая схема (COLOR_SCHEME) - РАСШИРЕНА
Автоматическое цветовое кодирование колонок по типам данных + динамическая генерация:

```python
COLOR_SCHEME = [
    {
        "group": "Process added fields",               # Группа для полей, добавляемых через MERGE
        "header_bg": "FFE6F2",                        # Цвет фона заголовка
        "header_fg": "2C3E50",                        # Цвет текста заголовка
        "style_scope": "header",                      # Область применения
        "sheets": ["REWARD", "REPORT", "TOURNAMENT-SCHEDULE"], # Применяемые листы
        "columns": [                                  # 🆕 Расширенный список колонок
            "CONTEST-DATA=>CONTEST_TYPE", "CONTEST-DATA=>FULL_NAME", 
            "CONTEST-DATA=>BUSINESS_BLOCK", "CONTEST-DATA=>BUSINESS_STATUS",
            "TOURNAMENT-SCHEDULE=>END_DT", "TOURNAMENT-SCHEDULE=>RESULT_DT", 
            "TOURNAMENT-SCHEDULE=>TOURNAMENT_STATUS",
            "REPORT=>CONTEST_DATE", "REPORT=>COUNT_CONTEST_DATE"
        ]
    }
]
```

---

## 🔧 Детальное описание функций

### 1. `setup_logger()`
**Назначение:** Инициализация системы логирования

**Логика работы:**
- Создаёт лог-файл с именем по дате и времени
- Настраивает вывод в файл и консоль одновременно
- Использует режим append для накопления логов
- Предотвращает дублирование обработчиков

**Пример использования:**
```python
log_file = setup_logger()
# Результат: LOGS2_2025-01-27_14-30.log
```

### 2. `read_csv_file(file_path)`
**Назначение:** Загрузка CSV-файла с автоматической обработкой ошибок

**Логика работы:**
- Читает файл с разделителем `;` и кодировкой UTF-8
- Все поля преобразует в строковый тип
- Логирует количество строк и колонок
- Возвращает DataFrame или None при ошибке

**Параметры:**
- `file_path` (str): Путь к CSV-файлу

**Возвращает:**
- `pd.DataFrame` или `None`

**Пример:**
```python
df = read_csv_file("/path/to/file.CSV")
if df is not None:
    print(f"Загружено {len(df)} строк, {len(df.columns)} колонок")
```

### 3. `safe_json_loads(s)`
**Назначение:** Безопасный парсинг JSON-строк с автоисправлением ошибок

**Логика работы:**
- Исправляет тройные кавычки `"""` → `"`
- Заменяет фигурные кавычки на стандартные
- Удаляет лишние запятые в конце объектов
- Исправляет отсутствующие двоеточия
- Пытается использовать `ast.literal_eval` при неудаче

**Параметры:**
- `s` (str): Строка для парсинга

**Возвращает:**
- `dict`, `list` или `None`

**Примеры обработки:**
```python
# Исправляет некорректные кавычки
safe_json_loads('{"key": """value"""}')  # → {"key": "value"}

# Исправляет отсутствующие двоеточия
safe_json_loads('{"key" "value"}')       # → {"key": "value"}

# Обрабатывает пустые значения
safe_json_loads("")                      # → None
```

### 4. `flatten_json_column_recursive(df, column, prefix=None, sheet=None, sep="; ")`
**Назначение:** Рекурсивное разворачивание JSON-колонок любой глубины

**Логика работы:**
- Парсит JSON-строки с помощью `safe_json_loads`
- Рекурсивно обходит все уровни вложенности
- Создаёт новые колонки с префиксами вида `prefix => key`
- Обрабатывает массивы, объекты и примитивные типы
- Сохраняет исходную колонку и создаёт развёрнутые

**Параметры:**
- `df` (pd.DataFrame): Исходный DataFrame
- `column` (str): Имя колонки для разворачивания
- `prefix` (str): Префикс для новых колонок
- `sheet` (str): Имя листа для логирования
- `sep` (str): Разделитель для массивов

**Возвращает:**
- `pd.DataFrame`: DataFrame с развёрнутыми колонками

**Пример разворачивания:**
```python
# Исходная колонка REWARD_ADD_DATA:
{"getCondition": {"nonRewards": [{"nonRewardCode": "ITEM_02"}], "employeeRating": {"minRatingBANK": "10"}}}

# Результат разворачивания:
# ADD_DATA => getCondition => nonRewards => [0] => nonRewardCode: "ITEM_02"
# ADD_DATA => getCondition => employeeRating => minRatingBANK: "10"
```

### 5. `add_fields_to_sheet(df_base, df_ref, src_keys, dst_keys, columns, sheet_name, ref_sheet_name, mode="value", multiply_rows=False)` - 🆕 ОБНОВЛЕНО
**Назначение:** Универсальное объединение данных по ключам (JOIN) с поддержкой размножения строк

**Логика работы:**
- Создаёт составные ключи из нескольких колонок
- Выполняет LEFT JOIN между таблицами
- Поддерживает режимы "value" (значения) и "count" (подсчёт)
- 🆕 **Размножение строк**: при `multiply_rows=True` создает строку для каждого совпадения
- Автоматически создаёт отсутствующие колонки с значением "-"
- Обрабатывает дублирующиеся ключи

**Новые параметры:**
- `multiply_rows` (bool): Размножать ли строки при множественных совпадениях

**Возвращает:**
- `pd.DataFrame`: DataFrame с добавленными колонками (возможно размноженный)

**Пример использования размножения строк:**
```python
# Если в источнике 3 награды для одного конкурса, то в результате будет 3 строки
df_result = add_fields_to_sheet(
    df_summary, df_rewards,
    src_keys=["CONTEST_CODE"], dst_keys=["CONTEST_CODE"],
    columns=["REWARD_CODE"],
    sheet_name="SUMMARY", ref_sheet_name="REWARDS",
    multiply_rows=True  # 🆕 Включено размножение
)
```

### 6. `calculate_column_width(col_name, ws, params, col_num)` - 🆕 НОВОЕ
**Назначение:** Вычисление ширины колонки на основе параметров и содержимого

**Логика работы:**
- Проверяет индивидуальные настройки для колонки (из MERGE_FIELDS)
- Применяет общие настройки листа (из INPUT_FILES)
- Поддерживает режимы: `"AUTO"` (по содержимому), число (фиксированная), `None` (ограниченная)
- Соблюдает минимальные и максимальные ограничения

**Параметры:**
- `col_name` (str): Имя колонки
- `ws`: Worksheet объект openpyxl
- `params` (dict): Параметры листа
- `col_num` (int): Номер колонки

**Возвращает:**
- `float`: Вычисленная ширина колонки

### 7. `generate_dynamic_color_scheme_from_merge_fields()` - 🆕 НОВОЕ
**Назначение:** Автоматическая генерация цветовой схемы на основе MERGE_FIELDS

**Логика работы:**
- Анализирует все правила в MERGE_FIELDS
- Группирует по целевым листам и источникам
- Автоматически назначает цвета из палитры (8 цветов)
- Создаёт правила для колонок, добавляемых через merge операции
- Логирует создание динамических схем

**Возвращает:**
- `list`: Список правил цветовой схемы для автоматически добавляемых колонок

**Пример генерируемых правил:**
```python
{
    "group": "MERGE: CONTEST-DATA -> REPORT",
    "header_bg": "FF9999",  # Светло-красный
    "header_fg": "2C3E50",
    "sheets": ["REPORT"],
    "columns": ["CONTEST-DATA=>CONTEST_TYPE", "CONTEST-DATA=>FULL_NAME"],
    "auto_generated": True
}
```

### 8. `collect_summary_keys(dfs)`
**Назначение:** Создание каркаса для итогового листа SUMMARY

**Логика работы:**
- Собирает все уникальные значения ключей из всех листов
- Создаёт все возможные комбинации ключей
- Включает "осиротевшие" коды (без связей)
- Обеспечивает полное покрытие всех данных

**Параметры:**
- `dfs` (dict): Словарь {sheet_name: DataFrame}

**Возвращает:**
- `pd.DataFrame`: Каркас с комбинациями ключей

**Пример результата:**
```python
# Создаёт строки для всех комбинаций:
# CONTEST_CODE | TOURNAMENT_CODE | REWARD_CODE | GROUP_CODE | GROUP_VALUE
# CONTEST_001  | TOUR_001        | REWARD_001  | GROUP_001  | VALUE_001
# CONTEST_001  | TOUR_001        | REWARD_002  | GROUP_001  | VALUE_001
# ...
```

### 9. `build_summary_sheet(dfs, params_summary, merge_fields)`
**Назначение:** Формирование итогового сводного листа

**Логика работы:**
- Создаёт каркас через `collect_summary_keys`
- Применяет все правила объединения из `merge_fields`
- Поддерживает размножение строк для SUMMARY
- Заполняет данные по ключам из всех источников
- Логирует процесс формирования

**Параметры:**
- `dfs` (dict): Словарь всех DataFrame
- `params_summary` (dict): Параметры листа SUMMARY
- `merge_fields` (list): Правила объединения для SUMMARY

**Возвращает:**
- `pd.DataFrame`: Готовый лист SUMMARY

### 10. `mark_duplicates(df, key_cols, sheet_name=None)`
**Назначение:** Маркировка дубликатов в DataFrame

**Логика работы:**
- Группирует строки по указанным ключам
- Подсчитывает количество дублей для каждой группы
- Создаёт колонку с пометкой вида "x2", "x3" и т.д.
- Пустая строка для уникальных записей

**Параметры:**
- `df` (pd.DataFrame): DataFrame для проверки
- `key_cols` (list): Колонки для проверки дублей
- `sheet_name` (str): Имя листа для логирования

**Возвращает:**
- `pd.DataFrame`: DataFrame с колонкой дублей

**Пример результата:**
```python
# Колонка "ДУБЛЬ: CONTEST_CODE_TOURNAMENT_CODE":
# "" (пусто для уникальных)
# "x2" (для дублей)
# "x3" (для тройных дублей)
```

### 11. `write_to_excel(sheets_data, output_path)`
**Назначение:** Запись всех листов в Excel с расширенным форматированием

**Логика работы:**
- Создаёт Excel-файл с помощью openpyxl
- Записывает каждый DataFrame на отдельный лист
- Применяет форматирование через `_format_sheet`
- 🆕 Использует индивидуальные настройки ширины колонок
- Устанавливает SUMMARY как активный лист
- Сохраняет файл с временной меткой

**Параметры:**
- `sheets_data` (dict): {sheet_name: (DataFrame, params)}
- `output_path` (str): Путь к выходному файлу

### 12. `_format_sheet(ws, df, params)` - 🆕 ОБНОВЛЕНО
**Назначение:** Форматирование отдельного листа Excel с точным управлением шириной

**Логика работы:**
- Устанавливает жирный шрифт для заголовков
- Выравнивает заголовки по центру с переносом строк
- 🆕 **Индивидуальный расчёт ширины** для каждой колонки через `calculate_column_width`
- Применяет цветовую схему через `apply_color_scheme` (включая динамическую)
- Закрепляет области и включает автофильтры
- Подробное логирование установки ширины

**Параметры:**
- `ws`: Worksheet объект openpyxl
- `df` (pd.DataFrame): Данные листа
- `params` (dict): Параметры форматирования (включая новые параметры ширины)

### 13. `apply_color_scheme(ws, sheet_name)` - 🆕 ОБНОВЛЕНО
**Назначение:** Применение статической и динамической цветовой схемы

**Логика работы:**
- Объединяет статическую схему (COLOR_SCHEME) и динамическую (из MERGE_FIELDS)
- Находит подходящие колонки для каждого листа
- Применяет цвета фона и текста к заголовкам
- Поддерживает автоматически генерируемые правила
- Логирует каждое применение цвета

**Параметры:**
- `ws`: Worksheet объект
- `sheet_name` (str): Имя листа

---

## 🔄 Алгоритм работы main()

### Пошаговый процесс:

1. **Инициализация** (`setup_logger()`)
   - Создание лог-файла с временной меткой
   - Настройка уровней логирования

2. **Загрузка данных** (цикл по `INPUT_FILES`)
   - Чтение каждого CSV-файла через `read_csv_file()`
   - Логирование результатов загрузки

3. **Разворачивание JSON** (цикл по `JSON_COLUMNS`)
   - Автоматическое обнаружение JSON-колонок
   - Рекурсивное разворачивание через `flatten_json_column_recursive()`
   - Создание новых колонок с префиксами

4. **Объединение данных** (`merge_fields_across_sheets()`) - 🆕 ОБНОВЛЕНО
   - Применение правил из `MERGE_FIELDS` (включая новые поля)
   - Поддержка размножения строк (`multiply_rows`)
   - Добавление полей между листами через `add_fields_to_sheet()`
   - Сохранение метаданных о ширине добавленных колонок

5. **Проверка дублей** (цикл по `CHECK_DUPLICATES`)
   - Маркировка дублей через `mark_duplicates()`
   - Создание колонок с пометками

6. **Формирование SUMMARY** (`build_summary_sheet()`)
   - Создание каркаса через `collect_summary_keys()`
   - Применение всех правил объединения (включая размножение)
   - Заполнение сводной таблицы

7. **Запись в Excel** (`write_to_excel()`) - 🆕 УЛУЧШЕНО
   - Создание Excel-файла с временной меткой
   - Индивидуальное форматирование каждого листа
   - Применение статической и динамической цветовых схем
   - Точное управление шириной всех колонок

8. **Финальное логирование** - 🆕 РАСШИРЕНО
   - Статистика по обработанным файлам
   - Информация о размножении строк
   - Детали установки ширины колонок
   - Время выполнения
   - Пути к выходным файлам

---

## 📊 Примеры использования

### Пример 1: Размножение строк при множественных наградах
```python
# Пример правила с размножением строк
{
    "sheet_src": "REWARD-LINK",
    "sheet_dst": "SUMMARY", 
    "src_key": ["CONTEST_CODE"],
    "dst_key": ["CONTEST_CODE"],
    "column": ["REWARD_CODE"],
    "mode": "value",
    "multiply_rows": True,           # 🆕 ВКЛЮЧЕНО: размножать строки
    "col_max_width": 40,            # Максимальная ширина
    "col_width_mode": "AUTO",       # Автоподбор ширины
    "col_min_width": 12             # Минимальная ширина
}

# Результат: если у CONTEST_001 есть 3 награды, то в SUMMARY будет 3 строки:
# CONTEST_001 | REWARD_001
# CONTEST_001 | REWARD_002  
# CONTEST_001 | REWARD_003
```

### Пример 2: Управление шириной колонок
```python
# В INPUT_FILES для общих настроек листа
{
    "file": "CONTEST-DATA (PROM) 2025-07-24 v4",
    "sheet": "CONTEST-DATA",
    "max_col_width": 120,            # Общая максимальная ширина
    "col_width_mode": "AUTO",        # Автоподбор для всех колонок
    "min_col_width": 8               # Общая минимальная ширина
}

# В MERGE_FIELDS для индивидуальных настроек добавляемых колонок
{
    "sheet_src": "CONTEST-DATA",
    "sheet_dst": "REPORT",
    "column": ["FULL_NAME"],
    "col_max_width": 80,             # Индивидуальная максимальная ширина
    "col_width_mode": 25,            # Фиксированная ширина 25
    "col_min_width": 10              # Индивидуальная минимальная ширина
}
```

### Пример 3: Динамическая цветовая схема
```python
# Автоматически генерируется на основе MERGE_FIELDS:
# Для правила CONTEST-DATA -> REPORT создается:
{
    "group": "MERGE: CONTEST-DATA -> REPORT",
    "header_bg": "FF9999",           # Автоматически назначенный цвет
    "header_fg": "2C3E50",
    "sheets": ["REPORT"],
    "columns": [                     # Автоматически определенные колонки
        "CONTEST-DATA=>CONTEST_TYPE",
        "CONTEST-DATA=>FULL_NAME", 
        "CONTEST-DATA=>BUSINESS_STATUS",
        "CONTEST-DATA=>BUSINESS_BLOCK"
    ],
    "auto_generated": True           # Маркер автогенерации
}
```

### Пример 4: Обработка сложного JSON
```python
# Исходные данные в колонке COMPLEX_JSON:
{
    "level1": {
        "level2": {
            "array": [
                {"key1": "value1", "key2": "value2"},
                {"key1": "value3", "key2": "value4"}
            ],
            "simple": "text"
        }
    }
}

# Результат разворачивания:
# COMPLEX_JSON => level1 => level2 => array => [0] => key1: "value1"
# COMPLEX_JSON => level1 => level2 => array => [0] => key2: "value2"
# COMPLEX_JSON => level1 => level2 => array => [1] => key1: "value3"
# COMPLEX_JSON => level1 => level2 => array => [1] => key2: "value4"
# COMPLEX_JSON => level1 => level2 => simple: "text"
```

---

## 📋 Техническое задание

### 1. Общие требования

**Цель проекта:** Создание универсального инструмента для автоматизированной обработки и анализа данных системы SPOD с возможностью масштабирования, точного контроля форматирования и адаптации к новым источникам данных.

**Основные задачи:**
- Автоматическая загрузка и обработка CSV-файлов
- Разворачивание сложных JSON-структур
- Объединение данных из множественных источников с поддержкой размножения строк
- Формирование аналитических отчётов с точным управлением форматированием
- Обеспечение прозрачности и отслеживаемости операций

### 2. Функциональные требования

#### 2.1 Обработка входных данных
- ✅ Поддержка произвольного количества CSV-файлов
- ✅ Автоматическое определение структуры данных
- ✅ Обработка файлов с разделителем `;` и кодировкой UTF-8
- ✅ Преобразование всех полей в строковый тип для унификации

#### 2.2 Разворачивание JSON-структур
- ✅ Автоматическое обнаружение JSON-колонок
- ✅ Рекурсивное разворачивание вложенных объектов
- ✅ Обработка массивов и примитивных типов
- ✅ Корректная обработка некорректных JSON-строк
- ✅ Сохранение исходных колонок и создание развёрнутых

#### 2.3 Объединение данных - 🆕 РАСШИРЕНО
- ✅ Универсальная система JOIN по любым ключам
- ✅ Поддержка составных ключей (несколько колонок)
- ✅ Режимы объединения: value (значения) и count (подсчёт)
- ✅ **НОВОЕ:** Размножение строк при множественных совпадениях
- ✅ **НОВОЕ:** Гибкое управление шириной добавляемых колонок
- ✅ Автоматическое создание отсутствующих полей

#### 2.4 Формирование отчётов - 🆕 УЛУЧШЕНО
- ✅ Создание сводного листа SUMMARY
- ✅ Автоматическое определение всех комбинаций ключей
- ✅ Включение "осиротевших" записей
- ✅ **НОВОЕ:** Динамическое цветовое кодирование на основе правил объединения
- ✅ **НОВОЕ:** Индивидуальное управление шириной каждой колонки

#### 2.5 Анализ качества данных
- ✅ Детекция и маркировка дубликатов
- ✅ Подсчёт количества дублей
- ✅ **НОВОЕ:** Расширенное логирование всех операций с детальной аналитикой

### 3. Технические требования

#### 3.1 Архитектура - 🆕 УЛУЧШЕНА
- ✅ Модульная структура с разделением логики
- ✅ Конфигурационный подход без изменения кода
- ✅ Универсальные функции для обработки данных
- ✅ **НОВОЕ:** Поддержка размножения строк в универсальных функциях
- ✅ **НОВОЕ:** Система управления метаданными колонок
- ✅ Расширяемость для новых источников

#### 3.2 Производительность
- ✅ Эффективная обработка больших объёмов данных
- ✅ Оптимизированное использование памяти
- ✅ **НОВОЕ:** Детальное логирование времени выполнения и коэффициентов размножения
- ✅ **НОВОЕ:** Оптимизированные алгоритмы расчёта ширины колонок

#### 3.3 Надёжность
- ✅ Обработка ошибок на всех этапах
- ✅ Валидация входных данных
- ✅ Безопасное завершение при критических ошибках

#### 3.4 Кроссплатформенность
- ✅ Работа на Windows, macOS, Linux
- ✅ Поддержка различных путей к файлам
- ✅ Независимость от операционной системы

### 4. Требования к выходным данным

#### 4.1 Excel-файл - 🆕 УЛУЧШЕН
- ✅ Отдельный лист для каждого источника данных
- ✅ Сводный лист SUMMARY с объединёнными данными
- ✅ **НОВОЕ:** Индивидуальное форматирование с точным управлением шириной
- ✅ **НОВОЕ:** Статическая и динамическая цветовые схемы
- ✅ Автофильтры для удобства анализа
- ✅ Закрепление областей

#### 4.2 Логирование - 🆕 РАСШИРЕНО
- ✅ Детальные логи всех операций
- ✅ Временные метки для каждой операции
- ✅ **НОВОЕ:** Логирование операций размножения строк
- ✅ **НОВОЕ:** Детальная информация об установке ширины колонок
- ✅ **НОВОЕ:** Статистика по динамической цветовой схеме
- ✅ Статистика по обработанным данным
- ✅ Сохранение логов по датам

### 5. Требования к конфигурации - 🆕 РАСШИРЕНЫ

#### 5.1 Гибкость настройки
- ✅ Добавление новых источников через конфигурацию
- ✅ Настройка правил объединения без изменения кода
- ✅ **НОВОЕ:** Индивидуальные параметры размножения строк для каждого правила
- ✅ **НОВОЕ:** Гибкое управление шириной колонок на уровне листов и правил
- ✅ **НОВОЕ:** Автоматическая генерация цветовых схем
- ✅ Настройка параметров форматирования

#### 5.2 Масштабируемость
- ✅ Поддержка неограниченного количества источников
- ✅ Простое добавление новых типов данных
- ✅ **НОВОЕ:** Автоматическая адаптация к новым полям в MERGE_FIELDS
- ✅ Расширение функциональности через конфигурацию

---

## 📈 История версий

### v1.0 (Базовая версия)
- ✅ Базовая загрузка CSV-файлов
- ✅ Простое объединение данных
- ✅ Вывод в Excel

### v1.1 (JSON-обработка)
- ✅ Добавлено разворачивание JSON-полей
- ✅ Поддержка вложенных структур
- ✅ Улучшенная обработка ошибок

### v1.2 (Сводные таблицы)
- ✅ Создание листа SUMMARY
- ✅ Автоматическое определение ключей
- ✅ Гибкие правила объединения

### v1.3 (Универсальные JOIN)
- ✅ Поддержка составных ключей
- ✅ Режимы value и count
- ✅ Универсальная система объединений

### v1.4 (Архитектурные улучшения)
- ✅ Расширяемая архитектура
- ✅ Подробное логирование
- ✅ Цветовое форматирование

### v1.5 (Стабильность и кроссплатформенность)
- ✅ Кроссплатформенная поддержка
- ✅ Новые правила объединения
- ✅ Улучшенная стабильность
- ✅ Обработка отсутствующих ключей

### v1.6 (Полная поддержка вложенных JSON)
- ✅ **Полная поддержка вложенных JSON** любой глубины
- ✅ **Новый алгоритм** автоматического поиска и разворачивания JSON-колонок
- ✅ **safe_json_loads** для гарантированной корректной обработки невалидных строк
- ✅ **Универсальный алгоритм** сбора всех возможных комбинаций ключей
- ✅ **Упрощённое масштабирование** — добавление новых связей без изменений кода
- ✅ **Поддержка append-логов** с расширенными шаблонами сообщений
- ✅ **Улучшенная обработка ошибок** и валидация данных
- ✅ **Оптимизированная производительность** для больших объёмов данных

### v1.7 (Текущая версия) - 🆕 Расширенные возможности обработки данных
- ✅ **Размножение строк** (`multiply_rows`) при множественных совпадениях
  - Новый параметр в MERGE_FIELDS для контроля поведения JOIN
  - Поддержка в функциях `add_fields_to_sheet` и `build_summary_sheet`
  - Детальное логирование операций размножения с коэффициентами

- ✅ **Гибкое управление шириной колонок**
  - Новые параметры в INPUT_FILES: `col_width_mode`, `min_col_width`
  - Индивидуальные настройки в MERGE_FIELDS: `col_max_width`, `col_width_mode`, `col_min_width`
  - Функция `calculate_column_width()` для точного расчёта
  - Режимы: `"AUTO"` (по содержимому), число (фиксированная), `None` (ограниченная)

- ✅ **Динамическая цветовая схема**
  - Автоматическая генерация через `generate_dynamic_color_scheme_from_merge_fields()`
  - 8-цветная палитра для разных источников данных
  - Интеграция с существующей статической схемой
  - Логирование создания динамических правил

- ✅ **Расширенное логирование операций**
  - Новые сообщения: `multiply_rows_start/result`, `column_width_set`, `dynamic_color_scheme`
  - Детальная аналитика размножения строк
  - Информация об установке ширины каждой колонки
  - Отслеживание генерации цветовых схем

- ✅ **Дополнительные поля из рабочей версии**
  - CONTEST-DATA → REPORT: +3 колонки (FULL_NAME, BUSINESS_STATUS, BUSINESS_BLOCK)
  - TOURNAMENT-SCHEDULE → REPORT: +1 колонка (TOURNAMENT_STATUS)
  - CONTEST-DATA → TOURNAMENT-SCHEDULE: 4 новых поля
  - REPORT → TOURNAMENT-SCHEDULE: CONTEST_DATE в режимах value и count
  - Обновлена цветовая схема для новых полей

- ✅ **Технические улучшения**
  - Сохранение метаданных о ширине добавленных колонок
  - Полная обратная совместимость с предыдущими версиями
  - Улучшенная производительность расчёта форматирования
  - Расширенная валидация параметров конфигурации

---

## 🛠️ Требования к системе

### Программное обеспечение
- **Python 3.8+** (рекомендуется 3.9+)
- **pandas** (для обработки данных)
- **openpyxl** (для работы с Excel)

### Установка зависимостей
```bash
pip install pandas openpyxl
```

### Системные требования
- **Оперативная память:** минимум 4 GB (рекомендуется 8 GB+)
- **Дисковое пространство:** минимум 1 GB свободного места
- **Операционная система:** Windows 10+, macOS 10.14+, Linux (Ubuntu 18.04+)

---

## 🚀 Запуск программы

### Базовый запуск
```bash
python main.py
```

### Результат выполнения
1. **Excel-файл** в директории `DIR_OUTPUT` с именем вида:
   ```
   SPOD_ALL_IN_ONE_2025-01-27_14-30-45.xlsx
   ```

2. **Лог-файл** в директории `DIR_LOGS` с именем вида:
   ```
   LOGS2_2025-01-27_14-30.log
   ```

### Структура выходного Excel-файла
- **SUMMARY** — сводный лист с объединёнными данными (активный)
- **CONTEST-DATA** — данные конкурсов с развёрнутыми JSON-полями
- **GROUP** — группировки
- **INDICATOR** — показатели
- **REPORT** — отчёты с добавленными полями из других источников
- **REWARD** — награды с развёрнутыми JSON-структурами и добавленными полями
- **REWARD-LINK** — связи наград
- **TOURNAMENT-SCHEDULE** — расписание турниров с добавленными полями
- **ORG_UNIT_V20** — организационные единицы
- **USER_ROLE** — роли пользователей
- **USER_ROLE SB** — роли пользователей СБ
- **EMPLOYEE** — сотрудники

---

## 🔧 Расширение функциональности

### Добавление нового источника данных

1. **Добавить файл в INPUT_FILES:**
```python
INPUT_FILES.append({
    "file": "NEW_SOURCE 2025-01-27 v1",
    "sheet": "NEW_SOURCE",
    "max_col_width": 60,
    "freeze": "B2",
    "col_width_mode": "AUTO",        # 🆕 Режим растягивания
    "min_col_width": 8               # 🆕 Минимальная ширина
})
```

2. **Добавить правило объединения:**
```python
MERGE_FIELDS.append({
    "sheet_src": "NEW_SOURCE",
    "sheet_dst": "SUMMARY",
    "src_key": ["NEW_KEY"],
    "dst_key": ["CONTEST_CODE"],
    "column": ["NEW_FIELD_1", "NEW_FIELD_2"],
    "mode": "value",
    "multiply_rows": False,          # 🆕 Контроль размножения строк
    "col_max_width": 50,            # 🆕 Индивидуальная максимальная ширина
    "col_width_mode": "AUTO",       # 🆕 Режим для добавляемых колонок
    "col_min_width": 10             # 🆕 Индивидуальная минимальная ширина
})
```

3. **Добавить проверку дублей:**
```python
CHECK_DUPLICATES.append({
    "sheet": "NEW_SOURCE",
    "key": ["NEW_KEY"]
})
```

### Настройка размножения строк

```python
# Пример: при добавлении всех наград для каждого конкурса
{
    "sheet_src": "REWARD-LINK",
    "sheet_dst": "CONTEST-DATA",
    "src_key": ["CONTEST_CODE"],
    "dst_key": ["CONTEST_CODE"], 
    "column": ["REWARD_CODE", "REWARD_TYPE"],
    "mode": "value",
    "multiply_rows": True,           # 🆕 ВКЛЮЧЕНО: создаст строку для каждой награды
    "col_max_width": 40,
    "col_width_mode": "AUTO",
    "col_min_width": 8
}
```

### Настройка управления шириной колонок

```python
# Различные режимы ширины колонок:

# 1. Автоматический подбор с ограничениями
"col_width_mode": "AUTO",          # По содержимому
"col_max_width": 50,               # Но не более 50
"col_min_width": 10,               # И не менее 10

# 2. Фиксированная ширина
"col_width_mode": 25,              # Точно 25 символов
"col_max_width": 100,              # Игнорируется
"col_min_width": 5,                # Игнорируется

# 3. Без растягивания
"col_width_mode": None,            # Без растягивания
"col_max_width": 30,               # Не более 30
"col_min_width": 8,                # Не менее 8
```

### Настройка JSON-разворачивания

1. **Добавить колонку в JSON_COLUMNS:**
```python
JSON_COLUMNS["NEW_SOURCE"] = [
    {"column": "NEW_JSON_FIELD", "prefix": "NEW_PREFIX"}
]
```

2. **Цветовая схема будет создана автоматически** через динамическую генерацию

---

## 📞 Поддержка и обратная связь

### Логирование проблем
При возникновении проблем проверьте:
1. **Лог-файл** в директории `DIR_LOGS`
2. **Консольный вывод** при выполнении
3. **Структуру входных файлов** на соответствие ожидаемому формату

### Типичные проблемы и решения

#### Проблема: Ошибка при загрузке CSV
**Решение:** Проверьте кодировку файла (должна быть UTF-8) и разделитель (должен быть `;`)

#### Проблема: Некорректное разворачивание JSON
**Решение:** Проверьте формат JSON в исходных данных, используйте `safe_json_loads` для отладки

#### Проблема: Отсутствующие данные в SUMMARY
**Решение:** Проверьте правила в `MERGE_FIELDS` и соответствие ключей между листами

#### Проблема: Неправильная ширина колонок 🆕
**Решение:** Проверьте параметры `col_width_mode`, `col_max_width`, `col_min_width` в INPUT_FILES и MERGE_FIELDS

#### Проблема: Слишком много строк после размножения 🆕
**Решение:** Проверьте параметр `multiply_rows` в соответствующих правилах MERGE_FIELDS, установите `False` если размножение не нужно

---

## 📄 Лицензия

Проект разработан для внутреннего использования в Сбербанке. Все права защищены.

---

**Версия документации:** 1.7  
**Дата последнего обновления:** 27 января 2025  
**Автор:** Команда разработки SPOD
